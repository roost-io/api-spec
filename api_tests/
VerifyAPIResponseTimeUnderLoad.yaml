# ********RoostGPT********

# Test generated by RoostGPT for test angelbroking-integration-demo using AI Type Vertex AI and AI Model claude-3-5-sonnet-v2
# 
# ROOST_METHOD_HASH=1e2cc70943
# ROOST_METHOD_SIG_HASH=060db45881
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Online Trading Platform API Testing\n\n  # User Onboarding and KYC',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Verify API response time under load',
#     steps: 'Given the API endpoint "/api/v1/trading/quotes"\n' +
#       'When I send 1000 concurrent GET requests\n' +
#       'Then all responses should be received within 2 seconds\n' +
#       'And the success rate should be greater than 99%\n' +
#       '\n' +
#       '  # Security Testing',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/api/v1"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 17
      name: "Load test trading quotes API"
  tls:
    rejectUnauthorized: false

scenarios:
  - name: "Verify trading quotes API response time under load"
    flow:
      - log: "Sending GET request to fetch market quotes"
      - get:
          url: "/trading/quotes"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - responseTime: 2000
      - think: 1
